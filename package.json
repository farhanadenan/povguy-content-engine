{
  "name": "povguy-content-engine",
  "version": "0.1.0",
  "description": "Generate daily POV Guy carousel + caption from market data",
  "private": true,
  "type": "module",
  "scripts": {
    "router": "node src/router.js",
    "generate": "node src/generator.js",
    "render": "node src/renderer.js",
    "build": "npm run router && npm run generate && npm run render"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2",
    "playwright": "^1.49.0"
  },
  "engines": {
    "node": ">=20"
  }
}
